#include "vault.h"

/* Reports whether the vault is open.
 * Ownership: none.
 * Side effects: none.
 * Error semantics: returns YES if open, NO if closed. */
int vault_is_opened(void) {
  // TODO: wire real vault implementation.
  return YES;
}
